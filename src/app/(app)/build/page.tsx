import { BuildView } from "@/components/surfaces/BuildView";

export const metadata = { title: "What to build — Heizen Discovery Tool" };

export default function BuildPage() {
  return <BuildView />;
}
