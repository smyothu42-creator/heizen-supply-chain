import { SourcesView } from "@/components/surfaces/SourcesView";

export const metadata = { title: "Sources — Heizen Discovery Tool" };

export default function SourcesPage() {
  return <SourcesView />;
}
