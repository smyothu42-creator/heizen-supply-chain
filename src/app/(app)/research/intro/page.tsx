import { IntroCallView } from "@/components/directions/CallEmphasis";

export const metadata = { title: "Intro call — Heizen Discovery Tool" };

export default function IntroCallPage() {
  return <IntroCallView />;
}
