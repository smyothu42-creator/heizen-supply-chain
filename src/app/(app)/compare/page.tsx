import { CompareView } from "@/components/surfaces/CompareView";

export const metadata = { title: "Compare — Heizen Discovery Tool" };

export default function ComparePage() {
  return <CompareView />;
}
